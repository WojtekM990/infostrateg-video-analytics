# k8s/dashboard.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  
  name: dashboard-deployment
  
spec:
  
  replicas: 1
  
  selector:
    
    matchLabels:
      
      app: dashboard
      
  template:
    
    metadata:
      
      labels:
        
        app: dashboard
        
    spec:
      
      containers:
        
      - name: dashboard-container
        
        image: ml-dashboard:v1
        
        imagePullPolicy: Never
        
        env:
          
        - name: DB_HOST
          
          value: "mysql-service"
          
        # Konfiguracja portu MySQL
          
        - name: MYSQL_DB_PORT
          
          value: "3306"
          
        ports:
          
        - containerPort: 8501

---

apiVersion: v1
kind: Service
metadata:
  
  name: dashboard-service
  
spec:
  
  type: LoadBalancer
  
  ports:
    
  - port: 8501
    
    targetPort: 8501
    
  selector:
    
    app: dashboard